# Local data boundary

No raw or generated source data belongs in the repository during Plan 1. Local
data directories are ignored so later ingestion work can preserve source files
without accidentally committing them.

The project's MIT License covers original project code and documentation. It
does not relicense source datasets. Every dataset retains its own license and
must carry its recorded provenance and license terms when introduced in a later
reviewed plan.
