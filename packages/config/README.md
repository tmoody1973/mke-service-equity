# Shared Configuration

This workspace owns reusable TypeScript configuration for the MKE Service Equity monorepo.

- `typescript/base.json` provides strict defaults for all TypeScript packages.
- `typescript/nextjs.json` adds the browser and Next.js settings used by the web application.
